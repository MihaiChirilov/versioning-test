mode: ContinuousDelivery
branches:
  main:
    regex: ^main$
    mode: ContinuousDelivery
    increment: Patch
  hotfix:
    regex: ^hotfix[/-]
    mode: ContinuousDeployment
    increment: Patch
    source-branches: ['main']
ignore:
  sha: []
